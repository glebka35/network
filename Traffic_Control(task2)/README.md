# Traffic Control (Task 2)

Используется docker-compose.

Создаются 4 клиента из разных геозон и сервер, сервер непрерывно рассылает пакеты на клиентов, клиенты посылают ICMP пакеты и SSH запросы на сервер. Состояние системы можно увидеть в логах докера.

Запуск: 
```
./run.sh
```
